<%= form_for(@x10module) do |f| %>
  <% if @x10module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@x10module.errors.count, "error") %> prohibited this x10module from being saved:</h2>

      <ul>
      <% @x10module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :unit_code %><br />
    <%= f.text_field :unit_code, :size => 5 %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 2 %>
  </div>
  <div class="field">
    <%= f.label :is_dimmable %><br />
    <%= f.check_box :is_dimmable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
